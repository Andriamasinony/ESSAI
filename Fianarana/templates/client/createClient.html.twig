{% extends 'base.html.twig' %}
{% form_theme formClient 'bootstrap_4_layout.html.twig' %}
{% block body %} 
{{ form_start(formClient)}} 
{{ form_row(formClient.num_compte) }}
{{ form_row(formClient.nom_client)}}
{{form_row(formClient.solde)}}

<button type="submit" class="btn btn-succes">Enregistrer</button>
<button type="submit" class="btn btn-primary">modifier</button>

{{ form_end(formClient) }}
     
{% endblock %}